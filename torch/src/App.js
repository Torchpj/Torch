import { BrowserRouter, Routes, Route } from "react-router-dom";
import TorchAni from "./pages/TorchAni/TorchAni";

function App() {
  return (
    <BrowserRouter>
    <TorchAni/>
        <Routes>

        </Routes>   
    </BrowserRouter>
  );
}

export default App;
